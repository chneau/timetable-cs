{
  "name": "timetable-cs",
  "private": true,
  "scripts": {
    "build": "dotnet build -c Release",
    "test": "dotnet test -c Release",
    "bench": "dotnet run -c Release --project . --bench",
    "start": "bun test"
  }
}
